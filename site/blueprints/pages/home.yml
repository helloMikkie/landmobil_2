title: One Pager
icon: 🧰

sections:
  parts:
    headline: Parts
    type: pages
    status: all
    image: false
    info: "{{ page.intendedTemplate }}"
    templates:
      - pager-headline
      - text
      - project
      - lecture
      - publication
      - media
      - pager-hero
  hero_image:
    type: files
    template: pager-reel
    layout: cards
    size: tiny
    info: "{{ file.caption }}"
    min: 1
    max: 5
